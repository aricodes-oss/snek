# frozen_string_literal: true

RSpec.describe Snek do
  it "has a version number" do
    expect(Snek::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
